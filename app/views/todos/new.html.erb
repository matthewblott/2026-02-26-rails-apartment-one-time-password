<% content_for :title, "New Todo" %>

<% content_for :header_controls do %>
  <div role="group">
    <a href="<%= user_todos_path %>" role="button">Todos</a>
    <button form="todo_form" formaction="<%= user_todos_path %>">Save</button>
  </div>
<% end %>

<%= render "form", todo: @todo %>
